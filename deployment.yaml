apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "engineers-guide-to-cerebral-palsy.fullname" . }}
  labels:
    {{- include "engineers-guide-to-cerebral-palsy.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "engineers-guide-to-cerebral-palsy.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "engineers-guide-to-cerebral-palsy.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}